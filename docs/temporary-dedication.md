# Dedicatoria temporal

La página principal muestra el texto **“(Por amor a Joselyn)”** inmediatamente después del pie público mediante la clase `temporary-dedication`.

La presentación se verificó en escritorio y móvil el 19 de agosto de 2026: permanece centrada, pequeña y separada del contenido principal. Para retirarla, basta con eliminar el párrafo correspondiente de `client/src/pages/Home.tsx` y la regla `.temporary-dedication` de `client/src/index.css`.
